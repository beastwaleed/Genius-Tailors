# Project Custom Rules for Genius Tailors

## Build & Git Rules
- Whenever new features, bug fixes, or code changes are implemented, run `npm run build` in `frontend/` to verify build correctness.
- Once verified, commit the changes to Git and push them to `origin/main` to maintain complete historical records and safety checkpoints.
